# Unique project key
sonar.projectKey=YOUR_PROJECT_KEY

# Project name
sonar.projectName=YOUR_PROJECT_NAME

# GitHub repository
sonar.sources=.

# Exclude files if needed
# sonar.exclusions=**/tests/**,**/*.md

# Optional: Language-specific settings
# sonar.language=java (or python, etc.)

# Authentication token (used in CI)
# Do NOT commit your token to public repo
# Use GitHub secrets instead
